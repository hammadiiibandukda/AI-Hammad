# CLAUDE.md

Guidance for [Claude Code](https://claude.com/claude-code) when working in this
repository.

## Project

**AI-Hammad** — a workspace repository. This file captures the context that
future sessions need: what the project is, how it's built, and the conventions
to follow. Keep it up to date as the project evolves.

## Status

The repository is a fresh scaffold. There is no build system, no dependencies,
and no source code yet. As those are added, update the sections below.

## Commands

_None yet._ Once a toolchain is chosen, record the common commands here so they
don't have to be rediscovered each session, for example:

- **Install:** `<command>`
- **Build:** `<command>`
- **Test:** `<command>`
- **Lint / format:** `<command>`

## Conventions

- Keep changes focused and commit with clear, descriptive messages.
- Update this file whenever project structure, tooling, or conventions change.
